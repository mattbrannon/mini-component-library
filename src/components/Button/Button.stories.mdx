import { Meta, Story } from '@storybook/addon-docs/blocks';
import Button from './Button';

<Meta
  title="Button"
  component={Button}
  argTypes={{
    size: {
      control: {
        type: 'select',
        options: ['small', 'medium', 'large'],
      },
    },
    variant: {
      control: {
        type: 'select',
        options: ['fill', 'outline', 'ghost', 'warning', 'danger', 'success', 'fancy']
      },
    },
  }}
/>

export const Template = (args) => <Button {...args} />;

# Button

<Story
  name="Default"
  args={{
    variant: 'fill',
    size: 'medium',
    children: 'Button'
  }}
>
  {Template.bind({})}
</Story>

